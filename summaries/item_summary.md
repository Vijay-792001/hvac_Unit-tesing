- Context: Module "item"; File "c_file_path". Purpose: drive a power LED and a bank of position LEDs on STM32F4 per traceable requirements (SWE-REQ-021/022/023/024/025/026/027/029/044).
- Purpose/behavior: Provide simple GPIO-based indication—power LED on PC0, up to 5 position LEDs on PC1–PC5—initializing hardware and updating LEDs based on a validated logical position.
- Public API:
  - StatusIndicator_Init(): Configures GPIOC pins (PC0–PC5) as push-pull outputs, no pull, low speed; turns power LED ON; turns all position LEDs OFF.
  - StatusIndicator_Update(position_valid, logical_position): Clears all position LEDs, then if position_valid != 0 and 1 <= logical_position <= STATUS_INDICATOR_NUM, lights exactly one LED at index (logical_position - 1). Position 0 or invalid inputs result in no position LED lit.
  - StatusIndicator_SetPowerLED(onoff): Sets power LED state (non-zero = ON, zero = OFF).
- Key flows:
  - Initialization sets the GPIO mode once for all relevant pins on port GPIOC, then establishes default LED states (power ON, positions OFF).
  - Update is idempotent per call: first turns all position LEDs OFF, then conditionally turns ON exactly one mapped LED.
  - Mapping: logical positions 1..5 map to s_led_pos_pins[0..4] (PC1..PC5). Position 0 is explicitly “no green LED.”
- Dependencies/interfaces:
  - Headers: status_indicator.h (declares API, provides STATUS_INDICATOR_NUM), stm32f4xx_hal.h (STM32 HAL).
  - HAL APIs: HAL_GPIO_Init, HAL_GPIO_WritePin.
  - GPIO config/types/macros: GPIO_InitTypeDef, GPIOC, GPIO_PIN_x, modes/pulls/speeds.
- Data/constants:
  - LED_POWER_PORT = GPIOC, LED_POWER_PIN = GPIO_PIN_0; LED_POS_PORT = GPIOC.
  - s_led_pos_pins: static const array {PC1..PC5}.
  - STATUS_INDICATOR_NUM governs loop bounds and valid position range (expected to match s_led_pos_pins length).
- Error handling/validation:
  - Minimal validation in Update: checks position_valid and bounds before indexing.
  - No return codes or error propagation; assumes HAL calls succeed.
- Notable assumptions/risks:
  - s_led_pos_pins fixed length 5; out-of-bounds risk if STATUS_INDICATOR_NUM > 5 or mismatch with array length.
  - Assumes GPIOC clock is enabled elsewhere (no __HAL_RCC_GPIOC_CLK_ENABLE here).
  - Hardcoded port/pins; no configurability; assumes active-high LEDs.
  - No concurrency/thread-safety guarantees; suitable for simple, single-threaded use.
  - No timing/blink patterns or debounce logic; purely sets steady states.
